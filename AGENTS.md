# 霁雪项目协作约定

- 所有源码注释、文档、测试说明和提交说明使用中文；代码标识符使用清晰的英文。
- Python 命令固定在 Conda 环境 `mycoder` 中执行。
- 项目核心是 Agent。代码优先短、直白、可运行，不为未来功能提前增加抽象层。
- 外部 SDK 只能出现在适配器内部，领域层不得导入 `anthropic`、Electron 或 MCP SDK。
- 每章只保留一个 `README.md`，不再拆分开发日志、链路文档和手测文档。
- 章节 README 用简短篇幅说明：当前成果、核心文件、完整链路、启动与测试、常见问题、变更记录、自测题与答案。
- 注释解释初学者不容易理解的概念、控制流和设计原因；不要给每一行代码做重复翻译。
- 自动化测试文件只保留在本地开发机，通过 `.gitignore` 排除，不加入 Git。
- 完成修改后不要自动提交 Git；先说明启动与测试方法，等待用户明确要求后再提交。
- 每个小步骤完成后，固定汇报：做了什么、如何启动和手动测试、下一步建议做什么。
- 新增或调整目录、文件时同步更新 `docs/PROJECT_STRUCTURE.md`。
- 不提交 API Key、访问令牌、用户对话、记忆数据或工具大结果。
- 未进入当前章节的能力不要实现；每次只完成一个小而可验证的闭环。
- 修改前检查工作区，修改后运行与改动相称的测试，并保留用户已有改动。
