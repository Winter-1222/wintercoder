# 霁雪开发路线

目标是一周做出一个能调用工具、会循环执行的简单 Agent Harness。原则是每一步都小、能运行、能手测，不提前建设后续章节。

## 当前进度

- 第 0 章：工程准备，已完成。
- 第 1 章：LLM + Electron 多轮聊天，已完成。
- 第 2 章：工具定义、一次执行、tool_result 回传和界面展示，已完成。
- 第 3 章：Agent Loop、停止条件、取消、并发和 Plan/Do 模式已完成。
- 第 4 章：System Prompt、动态环境提醒和 Prompt Cache 前缀已完成。
- 第 5 章：五层权限防御、三种权限模式和桌面确认已完成。
- 第 6 章：stdio 调用、并行后台连接、失败隔离和 UI 状态已完成；延迟加载和 HTTP 待完成。
- 第 7 章及以后：尚未开始。

写入和命令工具会按权限模式直接执行或显示确认卡片，硬拦截和路径沙箱始终优先。MCP 工具也进入同一个 Registry 和权限链。下一步实现 MCP 工具延迟加载和 ToolSearch，避免大量 schema 挤占上下文。

## 一周路线

1. **第 1 天：工程准备**
   建立 Python、Electron、Git 和本地测试环境。
2. **第 2 天：让 AI 开口说话**
   完成 LLM 适配器、多轮历史、流式界面和状态栏。
3. **第 3 天：工具系统**
   先实现最小工具接口、注册中心和一两个文件读取工具。
4. **第 4 天：Agent Loop**
   让模型请求工具、执行工具、把结果送回模型，直到完成或停止。
5. **第 5 天：提示词与权限**
   加入最小 System Prompt、只读/破坏性判断和用户确认。
6. **第 6 天：MCP**
   先接通一个 Server；延迟加载只做够用版本。
7. **第 7 天：上下文与记忆**
   先做会话保存，再决定是否继续做压缩和长期记忆。

## 后续章节

| 章节 | 最小交付 |
| --- | --- |
| 2. 工具系统 | 工具定义、注册、校验、执行结果 |
| 3. Agent Loop | 事件流、停止条件、工具调用循环、取消 |
| 4. System Prompt | 角色、环境、行为和输出规则 |
| 5. 权限 | 危险命令、路径范围、确认 |
| 6. MCP | transport、连接、发现、包装 |
| 7. 上下文 | 大结果落盘、手动/自动压缩 |
| 8. 记忆 | 会话 JSONL、项目指令、自动记忆 |
| 9. Subagents | 等主 Agent 足够稳定后再设计 |

每章只保留一个 README。进入下一章前，由用户先启动和手测当前章，再明确要求提交或继续。
