# 霁雪项目协作约定

- 所有源码注释、文档、开发日志、测试说明和提交说明均使用中文；代码标识符仍使用清晰的英文。
- Python 命令固定在 Conda 环境 `mycoder` 中执行。
- 外部 SDK 只能出现在适配器内部，领域层不得导入 `anthropic`、Electron 或 MCP SDK。
- 每个模块必须同时交付代码、自动化测试、开发日志、教学说明和手动测试记录。
- 不在仓库中提交 API Key、访问令牌、用户对话、记忆数据或工具大结果。
- 优先完成小而可验证的实现；未进入当前章节的能力只保留接口，不提前实现。
- 修改前检查工作区，修改后运行与改动相称的测试；保留用户已有改动。
