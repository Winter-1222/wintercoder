# 霁雪项目协作约定

- 所有源码注释、文档、开发日志、测试说明和提交说明均使用中文；代码标识符仍使用清晰的英文。
- Python 命令固定在 Conda 环境 `mycoder` 中执行。
- 外部 SDK 只能出现在适配器内部，领域层不得导入 `anthropic`、Electron 或 MCP SDK。
- 每个模块必须同时交付代码、自动化测试、开发日志、教学说明和手动测试记录。
- 默认读者是几乎零基础的初学者；章节教学不得假设读者已经掌握进程、异步、协议、框架或设计模式。
- 每章 `README.md` 必须包含推荐阅读顺序、零基础术语表、至少一条从用户操作到最终结果的完整运行链路，并逐步说明涉及文件、函数、输入、输出和存在理由。
- 教程必须明确区分“当前已经真实实现”“用于教学的模拟实现”“未来计划”，并提供可跟做练习、自测题与答案、按现象排错入口。
- 每章 `README.md` 的自测题必须紧跟参考答案；后续扩写章节时保留并同步更新，不能因为重构文档而删除。
- 每个源码文件都要能当作零基础教材阅读：文件顶部解释职责和所在链路；类、函数、方法说明“谁调用、输入、输出、为什么存在”；`reducer` 的每个 `case`、状态机分支、协议转换和异常降级必须逐项写中文注释。
- 注释重点解释概念、数据形状、控制流和设计原因；不能只把代码逐字翻译成中文。代码行为变化时必须同步维护相邻注释，避免注释与实现相反。
- 自动化测试文件只保留在本地开发机，通过 `.gitignore` 排除，不加入 Git；开发日志记录测试命令和结果。
- 完成修改后不要自动提交 Git；先向用户说明启动与测试方法，等待用户明确要求后再提交。
- 新增或调整目录、文件时同步更新 `docs/PROJECT_STRUCTURE.md`，写明每一项的职责。
- 不在仓库中提交 API Key、访问令牌、用户对话、记忆数据或工具大结果。
- 优先完成小而可验证的实现；未进入当前章节的能力只保留接口，不提前实现。
- 修改前检查工作区，修改后运行与改动相称的测试；保留用户已有改动。
